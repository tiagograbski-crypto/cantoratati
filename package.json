{
  "name": "tati-vanzan-site",
  "version": "1.0.0",
  "description": "Site premium da cantora Tati Vanzan — desktop e mobile",
  "private": true,
  "scripts": {
    "dev": "vite --host",
    "dev:desktop": "vite --host --open",
    "assets": "node scripts/download-assets.mjs",
    "images": "node scripts/optimize-images.mjs",
    "build": "vite build",
    "build:check": "node scripts/smoke-test.mjs",
    "preview": "vite preview --host",
    "ci": "npm run build && npm run build:check"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.2",
    "sharp": "^0.35.2",
    "tailwindcss": "^4.3.2",
    "vite": "^6.3.5"
  }
}
